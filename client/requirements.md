## Packages
framer-motion | Smooth animations for page transitions and UI elements
date-fns | Formatting dates for display (e.g., "2 hours ago")
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ['"IBM Plex Sans Arabic"', "sans-serif"],
  display: ['"IBM Plex Sans Arabic"', "sans-serif"],
}
RTL Direction is required for the entire application (Kurdish language).
